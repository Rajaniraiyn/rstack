[project]
name = "rstack"
version = "0.1.0"
description = "Tooling for R Stack, a portable collection of agent skills."
requires-python = ">=3.10"
dependencies = [
    "PyYAML>=6.0.3",
]

[tool.uv]
package = false